import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ReportBank from '../bank/Report'

const ReportAdmin = ({ navigation, route }) => {
    return (
        <View>
        </View>
    )
}

export default ReportAdmin

const styles = StyleSheet.create({})